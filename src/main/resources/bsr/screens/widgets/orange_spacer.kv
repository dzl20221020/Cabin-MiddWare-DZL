#:kivy 2.0.0

<OrangeSpacer>:
    canvas:
        Color:
            rgba: (float(233)/255, float(147)/255, float(34)/255,1)
        Rectangle:
            pos: self.x, self.y
            size: self.width, self.height
